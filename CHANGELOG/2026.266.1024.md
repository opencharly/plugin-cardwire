# 2026.266.1024 — feat: relocate the cardwire R10 witness bed from opencharly/charly

## Summary of changes

This PR relocates the `check-cardwire-local` R10 witness bed and its `kind:local` template out of `opencharly/charly`'s `charly.yml` into `opencharly/plugin-cardwire` — the repo that owns the `command:cardwire` plugin the bed exercises — so the bed composes the LOCAL out-of-tree `plugin-cardwire` candy (via the check-bed runner's dev-tree override) instead of a foreign-owned `@github` pin. WHY: a check bed must live beside the code it tests; the receiving repo previously declared no entities, so the bed is now the first entity in `charly.yml` (the schema-head `version:` stamp and the stale header comment are updated accordingly). The companion `opencharly/charly#625` removes the same witness from core in the same cutover.

- `charly.yml` (+47/-2) — adds the `check-cardwire-local-app` `kind:local` template (composes the LOCAL `plugin-cardwire` candy) and the `check-cardwire-local` `disposable: true` bed with its 3-check `plan:` (`e2e-cardwire-status`, `e2e-cardwire-list-na`, `e2e-cardwire-config-na`); bumps the schema stamp `version: 2026.240.1943` → `2026.249.2125`; sweeps the now-false "declares no entities of its own" header comment.
- `.gitignore` (+17/-0) — the standard generated-artifact ignore rules (`.build/`, `.containerignore`, `.dockerignore`, `.check/`, `output/`, editor scratch, migration backups) appended BELOW the two pre-existing entries (`/candy/plugin-cardwire/cardwire`, `pr-body.md`), which are preserved.
- `.github/workflows/deploy.yml` (+1/-1) — the CI-time pinned charly source checkout is bumped `v2026.242.1928` → `v2026.256.1316` (the tag carrying schema head `2026.249.2125`) so the CI gate accepts the new `version:` stamp.

## Change class

**candy/box-config** (non-docs: `charly.yml` entity + CI workflow pin + `.gitignore`).

**Closes / relates to:** relates to `opencharly/charly#625` (removes the relocated bed from `opencharly/charly`). The source-side removal is the matching leg of this one-phase cutover; #625 must merge or the relocated bed survives as a duplicate (R3/R5).

**Breaking change + rollback:** the bed/entity moves repos; rollback = revert this PR (and the companion `opencharly/charly#625` restores the core-side witness).

## How R10-tested

- **Gate:** `charly check run check-cardwire-local` (the R10 mode) + `charly box validate`, both on charly `2026.249.2125` (dev/worktree build), fresh clone at this tree (`/tmp/opencode/rem-cardwire-3`, head `4e1f2fc`), with `CHARLY_REPO_OVERRIDE="github.com/opencharly/plugin-cardwire=/tmp/opencode/rem-cardwire-3"`.
- **Bed result:** `check-cardwire-local` | local (`host: local`, `disposable: true`) | ok | steps=6 | 35s.
- **Step-count reconciliation:** `steps=6` is the R10 sequence (deploy-add, bring-up-members, check-live, update, cleanup, cleanup-members); the plan's **3** checks (`e2e-cardwire-status`, `e2e-cardwire-list-na`, `e2e-cardwire-config-na`) all execute inside the `check-live` step — the `check-live` log below shows all three PASS.
- **Changed path ran live:** yes — the changed artifact IS the bed definition, and its `check-live` step executed the three real host-side `${CHARLY_BIN} cardwire …` commands through the plugin's CLI dispatch (the proof).
- **Concurrency:** N/A — single host-local bed, one at a time.
- **Fresh rebuild:** the R10 `update` step (fresh rebuild) PASSED.

*1. Gate output — `charly box validate` + `charly check run check-cardwire-local`:*

```
$ cd /tmp/opencode/rem-cardwire-3 && charly box validate
WARNING: charly is a dev/worktree build (not installed at /usr/bin or /usr/local/bin); NOT loading the installed package's plugins from /usr/lib/charly/plugins (they may be built against a different charly version). Set CHARLY_PLUGIN_DIR to load plugins from a custom location.
charly box validate: OK — checked 2 candies, 1 deploy, 7 distros, 7 builders; 0 warnings, 0 errors

$ CHARLY_REPO_OVERRIDE="github.com/opencharly/plugin-cardwire=/tmp/opencode/rem-cardwire-3" charly check run check-cardwire-local
WARNING: charly is a dev/worktree build (not installed at /usr/bin or /usr/local/bin); NOT loading the installed package's plugins from /usr/lib/charly/plugins (they may be built against a different charly version). Set CHARLY_PLUGIN_DIR to load plugins from a custom location.
charly: resolving project (first run — may take a moment)...
charly: resolving project (first run — may take a moment)...
Resolved @github.com/opencharly/plugin-deploy-local -> v2026.242.0527 (latest tag)
Resolved @github.com/opencharly/plugin-vm -> v2026.262.0752 (latest tag)
plugin github.com/opencharly/plugin-deploy-local/candy/plugin-deploy-local: served from /home/atrawog/.cache/charly/plugins/github.com_opencharly_plugin-deploy-local_candy_plugin-deploy-local-cb999434bd120951 (build stamp 1a5f05ee92f5d34d); source /home/atrawog/.cache/charly/repos/github.com/opencharly/plugin-deploy-local@v2026.242.0527/candy/plugin-deploy-local
WARNING: charly is a dev/worktree build (not installed at /usr/bin or /usr/local/bin); NOT loading the installed package's plugins from /usr/lib/charly/plugins (they may be built against a different charly version). Set CHARLY_PLUGIN_DIR to load plugins from a custom location.
Removed /home/atrawog/.config/containers/systemd/charly-check-cardwire-local.container
Reloaded systemd user daemon
charly check run check-cardwire-local: [deploy-add] START (charly deploy add; log: .check/check-cardwire-local/2026.266.1011/deploy-add.log)
charly check run check-cardwire-local: [deploy-add] PASS after 3.964s [allowlisted=1] (log: .check/check-cardwire-local/2026.266.1011/deploy-add.log)
charly check run check-cardwire-local: [bring-up-members] START
charly check run check-cardwire-local: [bring-up-members] PASS after 0s
charly check run check-cardwire-local: [check-live] START (charly check live; log: .check/check-cardwire-local/2026.266.1011/check-live.log)
charly check run check-cardwire-local: [check-live] PASS after 15.99s [allowlisted=1] (log: .check/check-cardwire-local/2026.266.1011/check-live.log)
charly check run check-cardwire-local: [update] START (charly update; log: .check/check-cardwire-local/2026.266.1011/update.log)
charly check run check-cardwire-local: [update] PASS after 3.453s [allowlisted=1] (log: .check/check-cardwire-local/2026.266.1011/update.log)
charly check run check-cardwire-local: [cleanup] START (charly remove; log: .check/check-cardwire-local/2026.266.1011/cleanup.log)
charly check run check-cardwire-local: [cleanup] PASS after 11.485s [allowlisted=1] (log: .check/check-cardwire-local/2026.266.1011/cleanup.log)
charly check run check-cardwire-local: [cleanup-members] START
charly check run check-cardwire-local: [cleanup-members] PASS after 0s
charly check run check-cardwire-local: PASS (steps=6)
```

*2. Changed path executing — `.check/check-cardwire-local/2026.266.1011/check-live.log` (the three plan checks):*

```
$ cat .check/check-cardwire-local/2026.266.1011/check-live.log
WARNING: charly is a dev/worktree build (not installed at /usr/bin or /usr/local/bin); NOT loading the installed package's plugins from /usr/lib/charly/plugins (they may be built against a different charly version). Set CHARLY_PLUGIN_DIR to load plugins from a custom location.
charly: resolving project (first run — may take a moment)...
Resolved @github.com/opencharly/plugin-deploy-local -> v2026.242.0527 (latest tag)
Resolved @github.com/opencharly/plugin-vm -> v2026.262.0752 (latest tag)
charly: fetching git metadata for 1 repo(s) (first run — may take a moment)...
charly: git metadata cached.
plugin github.com/opencharly/plugin-deploy-local/candy/plugin-deploy-local: served from /home/atrawog/.cache/charly/plugins/github.com_opencharly_plugin-deploy-local_candy_plugin-deploy-local-cb999434bd120951 (build stamp 1a5f05ee92f5d34d); source /home/atrawog/.cache/charly/repos/github.com/opencharly/plugin-deploy-local@v2026.242.0527/candy/plugin-deploy-local
Local deploy: check-cardwire-local [host (local)]
  PASS  check command=cardwire status exits 0 and prints the deterministic report header (CLI dispatch, read-only)  [e2e-cardwire-status]  exit=0
  PASS  check command=cardwire list exits 0 with the graceful N/A report on a cardwire-less host (host-independent assertion)  [e2e-cardwire-list-na]  exit=0
  PASS  check command=cardwire config get exits 0 with N/A on a cardwire-less host (missing config file)  [e2e-cardwire-config-na]  exit=0

3 steps: 3 passed, 0 failed, 0 skipped
```

*3. Docs-only evidence:*

```
N/A — not a docs-only change.
```

## Attribution tier

**fully tested and validated** — the added witness's full R10 sequence executed live on a fresh clone at this tree on charly `2026.249.2125` (`steps=6`, all PASS incl. 3/3 plan checks, fresh `update` rebuild, clean teardown), and `charly box validate` reported 0 warnings / 0 errors.

## Harness rulebook compliance

- **R0 skills:** loaded `/charly-check:check` (bed model, R10, ADE), `/charly-internals:plugin` (plugin/provider model), `/charly-build:migrate` (schema stamp), and `/charly-internals:git-workflow` (PR-only landing) before editing.
- **R1 RCA + zero warnings:** the failures this move surfaced were root-caused and fixed — the receiving project needed the schema-head `version:` stamp (`2026.240.1943` → `2026.249.2125`) or project-resolve/CI failed, and the stale "declares no entities of its own" header was swept. Both gates report 0 errors / 0 warnings.
- **R2 no out-of-scope:** the source-side removal is the named companion `opencharly/charly#625` (added here, removed there in the same relocation); nothing is parked.
- **R3 no duplication:** the witness has ONE canonical home — the plugin's owning repo (the runner's dev-tree override maps the bed project's own superproject, so a foreign-owned candy is only exercised as in-development code beside its owner); `#625` deletes the core-side duplicate.
- **R4 no workaround:** no sleep/retry/shim; the witness runs the real `${CHARLY_BIN} cardwire …` on the host through the plugin's dispatch.
- **R4a product-first:** no documented behaviour was routed around.
- **R5 hard cutover:** the relocation IS the cutover; the charly-side identifier is removed in `opencharly/charly#625` — no Phase-2/TODO.
- **R6 git safety:** feature branch off `origin/main`; no force-push, no history rewrite; caught up with `origin/main` by merge; the body-freeze commit is an empty add-only commit.
- **R7 runtime gate:** the end-to-end `charly check run check-cardwire-local` R10 gate ran (not `go test` alone).
- **R8 artifact invariants:** `N/A — no Containerfile-generation or ai.opencharly.* label change.`
- **R9 binary == source:** the bed ran on the worktree dev binary `charly 2026.249.2125`; no new runtime OS dependency.
- **R10 disposable + coverage:** proven on a `disposable: true` host-local target from a fresh clone; the witness IS the coverage that fails without the plugin's dispatch.
- **RDD / ADE / SDD:** RDD — the dispatch premise was verified live (the run executes the real plugin command); ADE — the template composes the LOCAL plugin candy, and `kind:local` templates are exempt from the leaf-candy plan rule (`charly box validate` reports 0 errors); SDD — `N/A — no .cue edit.`
- **Hard cutover:** one atomic cutover per repo; the companion removal is the matching leg.
- **Kernel/plugin boundary law:** the witness MOVES OUTWARD out of core (`opencharly/charly`) into the plugin's owning repo — the direction the plugin-host end-state wants; no core/sdk movement.
- **Disposable-only autonomy:** the teardown/rebuild ran on a `disposable: true` bed.
- **Clean architecture + Go gates:** `N/A for Go — no Go changed.`
- **CHANGELOG:** `N/A — this repo's tag-on-merge writes CHANGELOG/<CalVer>.md from this PR body at merge (org convention).`

---

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (fully tested and validated)*