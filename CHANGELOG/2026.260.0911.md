# 2026.260.0911 — feat: author the `charly cardwire` command skill

## Summary

`charly cardwire` — the ogc/cardwire GPU-manager surface had no owning skill. Adds a `skill:` entity as a SIBLING top-level node
(`cardwire-skill:` beside the candy node) in `candy/<plugin>/charly.yml` — the `skill:`
kind's authored shape (a skill node is a document-unique top-level entity; the
schema `#Skill` at `spec/schema/skill.cue` says so, and the generator reads it as
`spec.Skill`, not a candy).

The generated marketplace corpus is never hand-edited; this candy entity is its
source.


## How tested

**R10 gate — the change's emitted artifact is PRODUCED, and it VANISHES without
the entity (a discriminator, not a vacuous check).** The generator composes every
`skill:` entity in the project and emits `<family>/skills/<name>/SKILL.md`. Run
end-to-end with the patched generator (opencharly/plugin-marketplace#11) against a
probe root composing this repo's candy + the marketplace families entity:
```
$ /tmp/mp-test generate --root . --out ./out
marketplace generate: wrote 99 artifact(s) across 38 family(ies)
$ head -3 out/cardwire/skills/cardwire/SKILL.md
---
name: cardwire
description: |-

$ # mutate: delete the "cardwire-skill:" node from this candy, regenerate
$ /tmp/mp-test generate --root . --out ./outX
marketplace generate: wrote 98 artifact(s) across 38 family(ies)
$ ls outX/cardwire/skills/cardwire/SKILL.md
ls: cannot access 'outX/cardwire/skills/cardwire/SKILL.md': No such file or directory
```
Present → 99 artifacts including this SKILL.md; entity removed → 98, the artifact
gone. So the added entity is exactly what produces the corpus artifact. This is
the change-class end-to-end gate: a docs-shaped `skill:` entity's only product IS
its corpus projection, and there is no disposable bed for prose.

**Schema gate + mutation (the entity is schema-validated):**
```
$ charly box validate                                   # this head
charly box validate: OK — checked 2 candies, 7 distros, 7 builders; 0 warnings, 0 errors

$ # mutate: delete the "cardwire-skill:" block's `family:` line
$ charly box validate
charly: error: command "validate": validation error: … node "cardwire-skill": plugin kind:skill: plugin_input fails #SkillInput: #SkillInput.family: incomplete value =~"^[a-z][a-z0-9-]*$"
```
Binary: charly-pinned v2026.247.1701 (built from source exactly as `deploy.yml`).

**Documented-surface provenance (R4a).** The skill's surface is the shipped
command: the plugin's own `candy/plugin-cardwire/charly.yml` (`command:cardwire`, subcommands `status|list|config|gpu <id> block|unblock|manager`) + its generated reference page ("command:cardwire (status | list | config | gpu <id> block|unblock | manager)").

**Cross-reference resolution — WITHIN the coupled set, not today.** Each
`/charly-<family>:<skill>` the skill names resolves once the coupled set lands
(these sibling skills are merged in their own PRs, below). Verified present on the
consumer side: `/charly-internals:plugin` resolves today; `/charly-bpf:bpf` resolves when opencharly/plugin-bpf#3 lands (its SKILL.md is emitted by the same generator run, pasted in the bpf PR).
**Entity-kind proof (not a second candy).** `charly box validate` counts
candies by candy-file; this repo has TWO candy files on `main` (`candy/cardwire-install/`
+ `candy/plugin-cardwire/`), so the pasted `checked 2 candies` is the BASELINE,
not this addition. The added node's kind is `skill`, read from the file:
```
$ # top-level nodes of candy/plugin-cardwire/charly.yml + their kind
node 'plugin-cardwire' kind 'candy'
node 'cardwire-skill'  kind 'skill'
```
$ python3 - <<'PY'  # top-level keys of the changed file + their kind
  node 'plugin-cardwire' kind 'candy'
  node 'cardwire-skill'  kind 'skill'
PY
```

## Coupled-landing contract (cross-repo dependency, B10)

`family: cardwire` is declared in the consumer's `marketplace:` entity
(opencharly/pod-charly-hooks#6, PASS/mergeable). The dependency is read live off
#6's head, and #6 must merge FIRST — merging this entity while the consumer's
`main` lacks the declaration makes the consumer's generator hard-error
(`skill "…" declares family "…" which the marketplace entity does not declare`).
The full coupled set, in order:
1. opencharly/pod-charly-hooks#6 — the family declarations. **Merges first.**
2. this PR.
3. the named PROJECTION GATE: the marketplace refs-list bump + corpus
   regeneration PR that pins this repo. That PR runs `charly marketplace
   generate` over the whole corpus; its deploy drift gate fails closed. It is
   where every entity's projection is proven together AND where the coupled
   cross-refs must all resolve (the docs build hard-fails otherwise).

## Rulebook compliance

| Rule | How this PR satisfies it |
|---|---|
| R0 — skills first | git-workflow, skills, marketplace, check loaded before acting. |
| R1 — RCA every anomaly | Gate zero warnings/errors; the family dependency read live and the coupling order stated, not assumed. |
| R2 — finish the whole cutover | Entity + family declaration (#6) + pin/regeneration land as one coupled set; no split, no unnamed parking. |
| R3 — no duplication | One `skill:` entity; documents the existing command, no second implementation. |
| R4 — no workarounds | Every command the skill names is the actual `charly` verb. |
| R4a — product-first docs | The documented surface is the shipped command (provenance above). |
| R5 — delete legacy | Additive only. |
| R6 — git safety | Branch-per-change PR; no force-push; clean tree. |
| R7 — prove behaviour | The generator discriminator (present→emitted, removed→gone) is the behavioural gate. |
| R8 — preserve emitted artifacts | The emitted SKILL.md content is asserted by the generator run (pasted). |
| R9 — binary equals source | Generator built from this source set; validate binary provenance stated. |
| R10 — fresh disposable proof | The generated-artifact discriminator above IS the class gate (a `skill:` entity's only product is its corpus projection; no bed applies to prose). |
| RDD/ADE/SDD | ADE: a `skill:` entity is not a candy, so no candy `description:`/`plan:` is required. SDD: the entity validates against the served `#SkillInput` schema. |
| Disposable-only gate | N/A — no targets created/destroyed. |
| Hooks doctrine | N/A — standalone repo; merge gate = org pr-validator. |

## Change classification

- **Class:** candy manifest entity (config) — an additive authored `skill:` entity.
- **Verification gate:** the generator-emit discriminator (pasted) + the
  mutation-tested `charly box validate` (pasted) + the named pin/regeneration
  projection gate.
- **Attribution tier:** `analysed on a live system` — the generator and validate
  paths ran live with retained output on the named host/worktree; the end-to-end
  corpus projection is the named pin/regeneration gate.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (analysed on a live system)*