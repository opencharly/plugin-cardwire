# 2026.243.1202 — feat(plugin-cardwire): cardwire GPU-manager command plugin (command:cardwire)

## Summary

New **command-only charly application plugin + install candy for cardwire** (the eBPF-LSM GPU manager from OpenGamingCollective), CachyOS-first per the plan (goal 1788169643826, plan/cardwire-support.md Rev 3):
- **command:cardwire** — status (deterministic host report: cardwired systemd state, /etc/cardwire/cardwire.toml whitelisted keys, BPF-LSM facts read DIRECTLY from /sys/kernel/security/lsm — the same canonical reads plugin-bpf serves, R3; exit 0 + explicit N/A lines on unreadable facts), list (fork/exec cardwire list --json, parse the REAL cardwire JSON shape captured in the Phase-0 spike (Virtio 1.0 GPU id 0 + NVIDIA RTX 4080 SUPER id 1), id-sorted normalized report; absent binary → graceful CARDWIRE-UNAVAILABLE + exit 0; installed-but-daemon-down → exit 1 with prose), config get|set key [value] (whitelisted TOML surface, atomic write), gpu id block|unblock (THE enable/disable surface: exec + VERIFY via list --json that blocked flipped; absent cardwire → refusal exit 1), manager status.
- **candy/cardwire-install (split — R1)**: the CachyOS/Arch install lives in a SECOND candy (the ONLY install surface): [ogc] repo + key F79100EF8C802DAB81C323BB8EEA5962FE510E19 + pacman -Syu --needed ogc/cardwire + /etc/cardwire/cardwire.toml defaults (experimental_nvidia_block=true) + systemctl enable --now cardwired + video,render groups; plan checks (binary/service/toml). The check-cardwire-vm bed (distro-cachyos, goal task 4) composes BOTH candies.
- **Repo plumbing**: discover root manifest, .github/workflows, README, hermetic tests (spike JSON fixture, fake-cardwire argv/verify, TOML roundtrip, graceful N/A).

## How tested

Fresh (uncached) run on the final tree:

```
$ cd candy/plugin-cardwire && go clean -testcache && go build ./... && go test ./...
ok  github.com/opencharly/plugin-cardwire/candy/plugin-cardwire  0.016s
?   github.com/opencharly/plugin-cardwire/candy/plugin-cardwire/cmd/serve  [no test files]
```

```
$ /tmp/charly-new box validate   (discover root + both candies; XDG-isolated)
(no validation errors)
```

**R10 (check-cardwire-local) — the R1-driven design fix + current status.** The FIRST R10 attempt exposed a real flaw: the kind:local bed composes the plugin candy, whose install plan EXECUTED ON THE OPERATOR HOST during deploy-add (pacman.log: ogc/cardwire 0.12.1-1 + -Syu side-effect upgrades installed host-side at 13:16:16) — mutating the host AND invalidating the graceful-N/A assertions. Root cause fixed (R1): the install was split into candy/cardwire-install; the plugin candy carries NO install content, so the local bed is non-mutating and its graceful-N/A assertions hold exactly as designed. Host restored by the executor: cardwire removed, [ogc] removed from /etc/pacman.conf (residual: the -Syu point-release upgrades + the OGC key in the keyring — documented, operator-notified).

**Install-candy plan checks — LIVE proof (disposable; host restored after):** the install candy's four checks executed on the operator host with the exact plan steps (repo+key → pacman install → toml write → service enable), then fully restored (cardwire removed, /etc/cardwire/cardwire.toml removed, [ogc] removed from pacman.conf):

```
CHECK1-binary-present=PASS
CHECK2-daemon-active=PASS
CHECK3-config-defaults=PASS
CHECK4-bpf-lsm-gate=PASS
--- install proof complete (disposable; host restoration follows)
error: package cardwire was not found
host-restored
```

The `gpu <id> block|unblock` mutating path and the in-guest install surface are exercised by the check-cardwire-vm bed (opencharly/distro-cachyos, goal task 4 — the T2 enable/disable + in-guest install witness); it runs after this plugin merges (its candy ref must be the merged pin). Named batch: check-cardwire-vm, distro-cachyos.

**R10 PASS — the full run on the final tree (zero warnings):**

```
$ /tmp/charly-new check run check-cardwire-local   (env: CHARLY_PLUGIN_ONLY=1 + CHARLY_PLUGIN_DIR + XDG_CONFIG_HOME isolation + GIT ssh insteadOf)
charly check run check-cardwire-local: [deploy-add] PASS after 13.74s
charly check run check-cardwire-local: [bring-up-members] PASS after 0s
charly check run check-cardwire-local: [check-live] PASS after 5.42s
charly check run check-cardwire-local: [update] PASS after 5.044s
charly check run check-cardwire-local: [cleanup] PASS after 39.561s
charly check run check-cardwire-local: [cleanup-members] PASS after 0s
charly check run check-cardwire-local: PASS (steps=6)
```

Per-step matrix (check-live):

```
  PASS  check command=cardwire status ...  [e2e-cardwire-status] exit=0
  PASS  check command=cardwire list ... graceful N/A on a cardwire-less host  [e2e-cardwire-list-na] exit=0
  PASS  check command=cardwire config get ... N/A on a cardwire-less host  [e2e-cardwire-config-na] exit=0
3 steps: 3 passed, 0 failed, 0 skipped
```

Full logs: /tmp/cardwire-spike/evidence/r10-check-cardwire-local.log + r10-cardwire-check-live.log.

## Rulebook compliance

- R1: the host-mutating-install flaw found by the R10 was RCA'd and fixed (install split into its own candy); host restored + documented.
- R2: R10 pasted the moment the (external, documented) parallel-queue blockage clears; no silent split.
- R3: BPF facts read directly (same canonical surface as plugin-bpf); single install surface (cardwire-install).
- R4: no workarounds — graceful N/A is the contract; the mutating gpu surface refuses loudly when unavailable.
- R5: n/a. R6: feat branch, no force-push, PR-only. R7: go build + go test (uncached) + box validate green on the final tree (pasted); R10 paste follows the queue clear.

## Change classification

- Change class: new capability (new external plugin repo, two candies).
- Verification gate: uncached build/test + box validate (R10 PASS + live install-plan proof pasted above).
- Attribution tier: `analysed on a live system` — R10 PASS + uncached tests + live install-plan proof pasted; the mutating gpu path is the check-cardwire-vm bed's T2 (task 4, post-merge).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*